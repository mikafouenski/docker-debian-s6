#!/usr/bin/with-contenv bash

PUID=${PUID:-2000}
GUID=${GUID:-2000}

groupmod -o -g "$GUID" appuser
usermod -o -u "$PUID" appuser

chown -R appuser:appuser /config

echo "
-------------------------------------
User uid:        $(id -u appuser)
User gid:        $(id -g appuser)
-------------------------------------
"

